# Snap link

## Setup
> [!NOTE]
> Our recommendation is to use `pnpm`, but you can use `npm` if you want.
- Install dependencies:
  ```bash
  pnpm install
  ```
- Run your project:
  ```bash
  pnpm run dev
  ```
  
